🚀 SQL AI Agent v2.1 目标

在 v2 的基础上新增 4 个关键能力：

1️⃣ SQL Streaming 输出（体验升级）
2️⃣ Schema 可视化（产品感）
3️⃣ Query History（可用性）
4️⃣ Prompt/Context 调试面板（开发者工具）
🏗️ v2.1 架构升级图
        ┌──────────────────────┐
        │   Next.js UI Layer   │
        └─────────┬────────────┘
                  ↓
     ┌─────────────────────────────┐
     │   Orchestrator (v2核心)     │
     └─────────┬───────────────────┘
               ↓
     ┌─────────────────────────────┐
     │ Context Engine (schema+biz) │
     └─────────┬───────────────────┘
               ↓
     ┌─────────────────────────────┐
     │   LLM Streaming Layer ⭐    │
     └─────────┬───────────────────┘
               ↓
     ┌─────────────────────────────┐
     │  UI Stream / Result Panel   │
     └─────────────────────────────┘
🧠 1️⃣ SQL Streaming（核心体验升级）
🎯 目标

从：

❌ 等 5 秒 → 一次性返回 SQL

升级成：

✅ AI 实时“打字输出 SQL”

📄 修改 API：route.ts

我们改成 Streaming Response

import { queryOrchestrator } from "@/core/orchestrator/queryOrchestrator";

export async function POST(req: Request) {
  const body = await req.json();

  const sql = await queryOrchestrator(body.prompt);

  const stream = new ReadableStream({
    start(controller) {
      const encoder = new TextEncoder();

      let i = 0;

      const interval = setInterval(() => {
        if (i >= sql.length) {
          clearInterval(interval);
          controller.close();
          return;
        }

        controller.enqueue(
          encoder.encode(sql[i])
        );

        i++;
      }, 10);
    }
  });

  return new Response(stream);
}
✨ 结果

UI 会变成：

SELECT *
FROM user_table
WHERE ...

逐字出现。

💅 2️⃣ 前端接 Streaming（关键）
📄 修改 page.tsx
"use client";

import { useState } from "react";

export default function Home() {
  const [input, setInput] = useState("");
  const [output, setOutput] = useState("");

  const run = async () => {
    setOutput("");

    const res = await fetch("/api/sql", {
      method: "POST",
      body: JSON.stringify({ prompt: input })
    });

    const reader = res.body?.getReader();
    const decoder = new TextDecoder();

    while (true) {
      const { value, done } = await reader!.read();
      if (done) break;

      setOutput((prev) => prev + decoder.decode(value));
    }
  };

  return (
    <main className="p-10">
      <textarea
        className="w-full border p-3"
        value={input}
        onChange={(e) => setInput(e.target.value)}
        placeholder="输入SQL需求"
      />

      <button
        className="mt-4 px-4 py-2 bg-black text-white"
        onClick={run}
      >
        生成SQL
      </button>

      <pre className="mt-6 bg-gray-100 p-4">
        {output}
      </pre>
    </main>
  );
}
🧭 3️⃣ Schema Viewer（产品化关键）
🎯 目标

让用户看到：

“这个 AI 是懂我数据的”

📄 新建：
/src/components/SchemaPanel.tsx
import { schema } from "@/core/context/schemaLoader";

export function SchemaPanel() {
  return (
    <div className="border p-4 rounded">
      <h2 className="font-bold mb-2">数据表结构</h2>

      {schema.tables.map((t) => (
        <div key={t.name} className="mb-2">
          <div className="font-semibold">{t.name}</div>

          <div className="text-sm text-gray-500">
            {t.columns.map((c) => (
              <div key={c.name}>
                {c.name} ({c.type})
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
  );
}
👉 在 page.tsx 加入
import { SchemaPanel } from "@/components/SchemaPanel";

然后布局：

<div className="grid grid-cols-2 gap-4">
  <div>
    {/* 输入 + 输出 */}
  </div>

  <SchemaPanel />
</div>
🕓 4️⃣ Query History（产品闭环）
📄 新建：
/src/core/history/store.ts
let history: any[] = [];

export function addHistory(item: any) {
  history.push(item);
}

export function getHistory() {
  return history;
}
👉 UI 加一个：
<div>
  <h2>历史查询</h2>
</div>
🚀 你的目标（v1 SQL Agent）

从：

❌ 用户输入 → LLM → SQL（瞎猜）

升级成：

✅ 用户输入 + 数据字典 + 表关系 + 业务定义 → LLM → 正确 SQL

🧱 v1 架构（你现在要实现的）
        ┌──────────────┐
        │  用户输入需求 │
        └──────┬───────┘
               ↓
     ┌────────────────────┐
     │  Schema Context层   │  ←（你现在要加的核心）
     │ - tables           │
     │ - columns          │
     │ - relations        │
     │ - metrics          │
     └────────┬───────────┘
               ↓
        ┌──────────────┐
        │   LLM 推理    │
        └──────┬───────┘
               ↓
        ┌──────────────┐
        │   SQL输出     │
        └──────────────┘
🧩 第一步：新增“数据大脑（Schema Layer）”

在项目里创建：

/src/lib/schema.ts
✨ 写入你的 v1 数据模型（先用 mock）
export const schema = {
  tables: [
    {
      name: "user_table",
      desc: "用户表",
      columns: [
        { name: "user_id", desc: "用户ID" },
        { name: "create_time", desc: "注册时间" },
        { name: "country", desc: "国家" }
      ]
    },
    {
      name: "order_table",
      desc: "订单表",
      columns: [
        { name: "order_id", desc: "订单ID" },
        { name: "user_id", desc: "用户ID" },
        { name: "pay_amount", desc: "支付金额" },
        { name: "pay_time", desc: "支付时间" }
      ]
    }
  ],

  relations: [
    {
      left: "user_table.user_id",
      right: "order_table.user_id",
      type: "1:N"
    }
  ],

  metrics: [
    {
      name: "新增用户",
      definition: "user_table.create_time 在时间范围内"
    },
    {
      name: "GMV",
      definition: "sum(order_table.pay_amount)"
    }
  ]
};
🧠 第二步：升级 Prompt（核心）

打开：

/src/app/api/sql/route.ts

替换成👇

✨ v1 SQL Agent Prompt
import OpenAI from "openai";
import { schema } from "@/lib/schema";

const client = new OpenAI({
  baseURL: "https://openrouter.ai/api/v1",
  apiKey: process.env.OPENROUTER_API_KEY,
});

export async function POST(req: Request) {
  const body = await req.json();

  const completion = await client.chat.completions.create({
    model: "openai/gpt-4o-mini",
    messages: [
      {
        role: "system",
        content: `
你是一个企业级数据仓库SQL生成助手（SQL Agent）。

你必须严格遵守以下数据仓库信息：

=== 数据表结构 ===
${JSON.stringify(schema.tables, null, 2)}

=== 表关系 ===
${JSON.stringify(schema.relations, null, 2)}

=== 业务指标定义 ===
${JSON.stringify(schema.metrics, null, 2)}

=== 强制规则 ===
1. 只能使用提供的表
2. 必须遵守 join 关系
3. 不允许编造字段
4. 不确定时必须返回：无法生成SQL
5. 只输出 SQL，不要解释
        `
      },
      {
        role: "user",
        content: body.prompt
      }
    ],
  });

  return Response.json({
    result: completion.choices[0].message.content
  });
}
🎯 第三步：你现在的能力变化

升级前：

用户：新增用户
AI：乱猜 SQL ❌

升级后：

用户：新增用户
AI：
✔ 知道 user_table
✔ 知道 create_time
✔ 知道时间过滤
✔ 输出正确 SQL
🧪 第四步：立刻测试（建议你马上做）

输入：

统计最近7天新增用户

应该输出类似：

SELECT COUNT(*)
FROM user_table
WHERE create_time >= CURRENT_DATE - INTERVAL 7 DAY;
🚀 v1 SQL Agent 的本质（你要记住）

你现在做的不是：

❌ 调大模型 API

而是：

✅ 构建“数据认知层 + LLM”